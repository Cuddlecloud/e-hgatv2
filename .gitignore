# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtual envs / uv
.venv/
venv/
.python-version

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage.xml
htmlcov/

# Editor / OS
.idea/
.vscode/
.DS_Store

# Vendored libs / scratch (regenerable, heavy)
.pylibs/
tmp/

# Secrets / local env (NEVER commit)
.env
*.env
secrets/

# Reference papers / advisor materials (local only, never commit)
*.pdf
refs/
typeset_equations/

# Experiment artifacts (heavy / regenerable). Golden references are tracked.
experiments/**/runs/
experiments/**/*.pt
experiments/**/*.png
!tests/data/golden/**
